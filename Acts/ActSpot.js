function ActSpot() {
    this.content = "Act 1";

    this.input = function() {
        if (
            mouseIsPressed
            && collidePointEllipse(mouseX, mouseY, (width/2), 150, 200, 150
        ) {
            
        }
    }
}